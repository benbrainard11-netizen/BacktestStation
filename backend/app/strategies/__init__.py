"""User-facing strategy implementations.

Strategies live under `examples/` for now. When more land we'll add a
registry, but with one example strategy + Ben's eventual real one a
plain import is sufficient.
"""
