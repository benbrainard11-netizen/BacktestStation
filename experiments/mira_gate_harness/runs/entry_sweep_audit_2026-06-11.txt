{
  "summary": "Adversarially verify the entry_sweep.py engine for look-ahead, cost parity, reuse fidelity, and framing errors",
  "agentCount": 5,
  "logs": [],
  "result": {
    "n_raw": 31,
    "synthesis": "All findings verified against the code (entry_sweep.py, realized_r.py, live_engine\\engine\\signal.py:20/89, run.py:159-238, risk.py:76-108, feed.py:149-162, gate.py). Confirmed findings, deduplicated, ordered:\n\n**BLOCKER 1 — decision_ts anchor mismatch: \"touch10_lag6 = current live\" is false.**\nVerified: live constructs ReclaimTrade with `decision_ts = trigger_ts_utc` (run.py:190) and signal.py:89 cancels when `ts - decision_ts > MAX_ENTRY_WAIT`, so live's window is trigger+10m regardless of detection lag (effective post-arm window = max(0, 10m − lag); lag>10m arms cancel on the first quote). The sweep sets `decision_ts = arm_ts = trig + lag` (entry_sweep.py:71, 96), giving every lag>0 policy a full fresh window live cannot have — touch10_lag6 hunts to trig+16m, touch10_lag12 should be dead-on-arrival, touch20/30 run to trig+26/36m. Inflates the \"current live\" baseline, voids the lag-sensitivity rows, contradicts the docstring (lines 3-6); the validation gate can't see it because at lag 0 the two anchors coincide.\nFix: model live with `decision_ts=trig_ts` (window = max(0, 10m − lag)) for all \"current live\"/lag rows; label arm-anchored waits as a PROPOSED engine change requiring a one-line live edit.\n\n**BLOCKER 2 — independent deduped replay ignores live one-per-symbol blocking AND live retries — two opposite uncontrolled biases on the exact market-vs-touch delta.**\nVerified: live blocks any same-symbol candidate while armed OR open (run.py:168 \"already_active\"; risk.py:92 \"position_already_open\", max_position_per_symbol=1), but the sweep drives every arm independently (entry_sweep.py:143-158) — overstates high-fill policies (market_lag6, touch20/30); auditor quantification ~30% of arms inside a prior arm's occupancy window, ~25% of total R. Conversely the dedup `groupby(OPP).head(1)` (entry_sweep.py:130) denies the live retry on later triggers of the same setup event (~34% of events multi-trigger), penalizing touch policies only. The two biases do not cancel in any controlled way.\nFix: one sequential per-symbol replay over the UN-deduped gated trigger stream with armed/open occupancy state (skip triggers while a prior simulated arm/position is active) — fixes both at once.\n\n**BLOCKER 3 — validation gate cannot bind: intersection-only comparison, exercised only at the degenerate config.**\nVerified: entry_sweep.py:168 `both = v[net_r.notna() & cached_r.notna()]` drops every fill-status mismatch (no fill-count/fill-set assertion, no min-n guard — can pass 100% vacuously), and the sole validated policy (lag=0, wait=10 == signal.py:20 default) makes the lag arithmetic, the MAX_ENTRY_WAIT monkeypatch, the market seed, and the hi truncation all no-ops or unexercised. The 0.02R/95% tolerance can absorb ~30 divergent trades on bit-identical machinery. Passing the gate validates none of the rows the decision is made on.\nFix: assert fill-SET agreement (count + symmetric difference, whitelist known window-tail cases), compare exit reasons, tighten matched-row tolerance to ~1e-9.\n\n**MATERIAL 4 — constant 6m lag misrepresents the right-skewed live lag distribution (p90 ~13m).**\nVerified: one fixed lag per policy (entry_sweep.py:54-63, 71) vs the docstring's own median 5.7m/p90 13m. Under deployed trigger-anchored semantics (Blocker 1) roughly half the arms get less window than simulated and the >10m tail gets zero, so even the corrected baseline overstates live conversion; the lag ladder exists only for touch10, so a mixture-weighted estimate can't be reconstructed for the challengers; fixed lag also erases lag-vs-tape-state correlation.\nFix: sample per-arm lags from the measured live distribution (per-arm lags exist in mira_candidates.jsonl) under deployed semantics; at minimum add lag13 rows and run the ladder for every policy.\n\n**MATERIAL 5 — 65% of the arm set is the gate's train window; headline ALL row and Feb–May monthly table are in-sample.**\nVerified: WINDOWS includes \"train\" (entry_sweep.py:49), pooled in the ALL row (:180-191) and monthly table (:193-203); gate threshold = q75 of train scores (gate.py:7), label anchored at decision time (favoring touch-style anchoring). Known Feb–May in-sample caveat. Absolute levels and the pooled ranking should not anchor the live decision; honest OOS is only ~169 arms.\nFix: print the headline and monthly tables OOS-only (jan_oos + oos_holdout) and label train rows in-sample.\n\n**MATERIAL 6 — no uncertainty or multiplicity control: 8 policies ranked on point estimates of a heavy-tailed statistic.**\nVerified: report prints means/sums only (entry_sweep.py:179-203); OOS n=64/105 gives R/arm SEs on the order of plausible policy deltas; clustered same-symbol arms shrink effective n; best-of-8 selection guarantees winner's-curse inflation. The paired design (identical arm set) is unused.\nFix: compute paired per-arm deltas vs touch10_lag6 with a block bootstrap CI (block by symbol-day) and decide on the delta CI.\n\n**MINOR 7 — market-on-arm fills at the same quote it observes, with the same 1-tick slip as touch.**\nVerified: no lookahead (seed scan requires ts >= arm, finite quotes; fill pays the ask via signal.py:98-99), but zero order latency + identical slip is optimistic for a market order into post-reclaim momentum — and it biases toward the challenger under decision.\nFix: re-run market mode filling at the first quote >= arm + realistic latency (or the second post-arm quote) and confirm the ranking survives.\n\n**MINOR 8 — hi-bound truncation scores entered-but-still-open trades 0R (\"expired_eod\"), with a different censoring window than the cache.**\nVerified: loop capped at hi = arm + (wait+65)m (entry_sweep.py:74) falls through to \"expired_eod\" even when IN_POSITION (:117), only a ~5m cushion after worst-case hold expiry; realized_r.drive iterates to the day-group array end (realized_r.py:76, :109), so lag0 one-sided fills exist in both directions — exactly the rows the gate's intersection filter drops. Tiny population on dense futures tape but unquantified.\nFix: extend hi by +60m or mark-to-market at the last quote with reason \"time\"; print entered-but-expired_eod counts per policy.\n\n**MINOR 9 — per-trade output omits entry_ts/entry_px/exit_ts, and saved idx is non-unique across windows.**\nVerified: result rows (entry_sweep.py:155-158) discard act.price/trade.entry_ts; concat at :132 keeps colliding per-window RangeIndexes into the saved parquet (:212-213). The lag claims are unauditable from the artifact, and the fixes for Blockers 2/6 need these timestamps; idx joins across windows silently mis-pair.\nFix: return and persist entry_ts/entry_px/exit_ts/exit_px from drive_entry and key rows by (window, idx) or reset the index.\n\n**MINOR 10 — R-denominator heterogeneity: market entries mid-momentum re-anchor tighter stops, so cross-policy R/arm is dollar-faithful only under $/R sizing.**\nVerified mechanism (entry_sweep.py:107-110 -> feed.py:149-162 -> signal.py:72-85); costs scale correctly with 1/risk_pts. Mitigated by live's micro-based ~$75/R sizing (run.py:220-227) but integer rounding/caps leave residual lumpiness. Interpretation caveat only.\nFix: print per-policy risk_pts distribution; if market_lag6's median differs materially from touch10_lag6's, also report dollar P&L per arm.\n\n**REJECTED (spurious/pedantic):**\n- \"cancel_stop scored 0R but live already holds a filled position\" — dead path: append_raw precedes on_quote so local_extreme always includes the entry quote, guaranteeing ref is non-None and risk >= spread + 2-tick buffer (>0) for both directions; and live sends the order only AFTER reset_stop + allow_entry succeed (run.py:236-238), so live holds nothing either.\n- sys.path stdlib `signal` shadowing — real latent crash hazard, but it would crash, not silently distort the comparison.\n- \"target\" slip exemption in net_r — unreachable under trail_2R, which every policy and the cache use; dormant.\n- All \"verified clean\" notes (pre-arm buffer gating, stop-window parity, monkeypatch hygiene, monthly fillna consistency, decision_ts touching only the wait clock, gate threshold provenance) — confirmations, not findings."
  }
}