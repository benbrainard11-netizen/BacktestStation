"use client";

import { PageStub } from "@/components/atoms";

export default function PropFirmsPage() {
  return (
    <PageStub
      title="Firm Rules"
      blurb="Prop-firm rule profiles. Built-in presets (Topstep, FTMO, etc.) plus any custom firms you've configured."
    />
  );
}
