"use client";

import { PageStub } from "@/components/atoms";

export default function StrategyBuilderPickerPage() {
  return (
    <PageStub
      title="Strategy Builder"
      blurb="Pick a strategy to enter the visual feature builder for one of its versions."
    />
  );
}
