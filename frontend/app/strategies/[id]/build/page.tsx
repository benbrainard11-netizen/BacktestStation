"use client";

import { PageStub } from "@/components/atoms";

export default function StrategyBuildPage() {
  return (
    <PageStub
      title="Strategy Build"
      blurb="Composable feature builder for the selected strategy version. Compose entry / exit / stop / target rules from the feature pantry."
    />
  );
}
