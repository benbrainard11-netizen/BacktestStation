"use client";

import { PageStub } from "@/components/atoms";

export default function ComparePage() {
  return (
    <PageStub
      title="Compare Runs"
      blurb="Side-by-side metrics comparison of 2-4 backtest runs. Winner highlighting per row."
    />
  );
}
