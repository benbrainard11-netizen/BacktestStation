"use client";

import { PageStub } from "@/components/atoms";

export default function TradeReplayPage() {
  return (
    <PageStub
      title="Trade Replay"
      blurb="Tick-by-tick TBBO playback around a single live trade. Pick a run, pick a trade, watch the bid/ask tape with bar overlay."
    />
  );
}
